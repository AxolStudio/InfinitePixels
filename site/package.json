{
  "name": "infinite-pixels-site",
  "version": "1.0.0",
  "description": "website portion of infinite pixels",
  "main": "index.html",
  "scripts": {
    "build": "hugo --gc --minify --cleanDestinationDir ${SITE_BASE_URL:+--baseURL \"$SITE_BASE_URL\"}",
    "watch:css": "sass --watch assets/scss:.",
    "watch": "npm run watch:css",
    "script": "npm run build && npm run watch",
    "run": "hugo server -D --disableFastRender --baseURL http://localhost:1313/ ",
    "serve": "hugo server -D --disableFastRender --baseURL http://localhost:1313/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bignumber.js": "^9.1.2",
    "bootstrap": "^5.3.3"
  }
}
